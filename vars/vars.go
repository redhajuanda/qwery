package vars

var (
	// TagKey is the key used to identify the struct tag
	TagKey = "qwery"
	// DefaultPaginationPerPage is the default number of items per page for pagination
	DefaultPaginationPerPage = 10
	// DefaultPaginationPage is the default page number for pagination
	DefaultPaginationPage = 1
)
